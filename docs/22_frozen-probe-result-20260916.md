# 고정 프로브 음량·경로 검사 결과 — 2026-09-16

## 결론

실제 추론 경로 적용의 수치 일치는 이번 입력에서 확인했다. 원래 음량에서도 개발 File EER 개선은 유지됐지만 낮은 음량에서 EER이 악화됐다. 가중치는 유망한 후보로 유지하며, 전체 오디오에 바로 적용하는 제출 변경은 하지 않는다. 다음 병목은 추가 음악 설정 탐색보다 음성·혼합 파일의 회귀와 성분 정답을 갖춘 전체 Score 검증이다.

근거: `C:\Users\kimdo\Downloads\frozen_probe_results.zip`.
SHA-256: `32a00ef590001df168a00f964acd169506bdd10151fc0d30f892e5d54fd1a63f`.
입력 설계: `docs/21_frozen-probe-check-20260916.md`.

## 실측

| 항목 | 수치 | 출처 URL | 기준연도 | 비고 |
|---|---|---|---|---|
| reference | 기존 EER 0.75 / 프로브 EER 0.125, 프로브 AUC 0.859375 | 로컬 결과 ZIP summary.json·predictions.csv | 2026 | 학습 전처리와 같은 입력 |
| float_reference | 기존 EER 0.75 / 프로브 EER 0.125, 프로브 AUC 0.859375 | 같은 결과 ZIP | 2026 | 저장 형식 대조 |
| original_level | 기존 EER 0.75 / 프로브 EER 0.125, 프로브 AUC 0.875 | 같은 결과 ZIP | 2026 | 원래 음량 |
| quiet | 기존 EER 0.625 / 프로브 EER 0.25, 프로브 AUC 0.890625 | 같은 결과 ZIP | 2026 | reference 진폭의 0.1배 |

위 수치는 외부 벤치마크가 아니라 사용자 GPU 실험 결과다. 원본은 개발 데이터 16개이며 변형 입력 64개가 독립 음원 64개라는 뜻은 아니다. quiet에서 AUC가 높아졌더라도 EER은 악화됐다. 대회는 FILE에 AUC가 아니라 EER을 사용하므로 AUC 상승만으로 개선이라 판단하지 않는다. 표본이 작아 낮은 음량 전반의 성능 저하 정도를 확정할 수는 없다.

## 검증

- ZIP CRC 통과. summary.json과 64개 예측, 실행 로그 존재.
- 반환 manifest와 원래 실행 번들 manifest가 동일.
- 예측 ID·정답·조건·행 수 대조, 확률 범위·유한값 검사 통과.
- 조건별 기존 모델 및 프로브 EER/AUC를 실제 예측에서 재계산해 summary.json과 일치.
- 단독 프로브와 실제 추론 경로 FILE 출력의 최대 차이는 0.0. 저장된 pipeline_gap도 직접 계산한 차이와 일치.
- reference와 float_reference의 프로브 예측 최대 차이도 0.0. 이 입력에서 float WAV 저장 자체로 점수가 바뀐 증거는 없다.
- 실행 코드의 FILE 외 네 확률 전후 비교 검사도 중단 없이 통과했다. 다만 네 확률 원값을 결과 CSV에 저장하지 않아 반환 ZIP만으로 그 비교를 다시 계산할 수는 없다.
- 모델 설치 및 전체 실행은 Colab 환경이다. OS 수준 오프라인 격리나 L4 평가 서버 자원 제한 검증을 대신하지 않는다.

## 다음 판단

이 결과만으로 음량 정규화를 전체 제출 입력에 추가하지 않는다. 존재 탐지·음성 탐지에 대한 영향과 작은 신호/잡음을 과도하게 확대할 가능성을 실제 데이터로 확인해야 한다. 확인셋에서 선택된 임계값이나 가중치를 재튜닝하지 않는다.

다음은 고정 프로브를 별도의 실제 음성·생성 음성·혼합 조건에 적용해 파일 판별 회귀를 확인하는 실험이다. 음악 중심으로 학습한 FILE 프로브가 음성 파일에 잘못 적용될 가능성이 있다. 이를 확인한 뒤 적용 범위와 전체 산식을 검증할 수 있다. 필요한 데이터 출처·성분 정답이 확보되기 전에는 총점이나 새 제출물의 성능을 확정하지 않는다.

이번 작업에서 추가 학습·새 가중치·제출 파일 변경·대회 제출은 수행하지 않았다.

AI 활용 기록: 2026-09-16, Codex가 사용자 고정 프로브 실험 결과, manifest, 예측, 경로 차이를 대조하고 EER/AUC를 재계산해 기록했다.

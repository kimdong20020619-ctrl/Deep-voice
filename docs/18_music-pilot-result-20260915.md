# 실제 음악 파일 진단 결과 — 2026-09-15

## 판단

이번 소규모 표본에서 기존 direct/max보다 개선된 후보는 나오지 않았다. direct/mean 또는 fusion/max로 제출 설정을 바꿀 근거가 없다. 현재 제출 ZIP과 추론 설정을 유지한다. 이것은 direct/max가 모든 데이터에서 최적이라는 결론이 아니다.

근거 파일: `C:\Users\kimdo\Downloads\music_pilot_results.zip`. 표본 설계와 한계는 `docs/17_music-pilot-ready-20260912.md` 참조.

## 실제 결과

| 항목 | 수치 | 출처 URL | 기준연도 | 비고 |
|---|---|---|---|---|
| direct/max | File EER 0.40, AUC 0.63 | 로컬 결과 ZIP의 summary.csv 및 direct_max_predictions.csv | 2026 | 현재 설정, 실제 20개 파일 |
| direct/mean | File EER 0.40, AUC 0.62 | 같은 ZIP의 direct_mean_predictions.csv | 2026 | EER 개선 없음 |
| fusion/max | File EER 0.60, AUC 0.44 | 같은 ZIP의 fusion_max_predictions.csv | 2026 | 이 표본에서 악화 |

외부 출처의 성능 수치가 아니라 사용자 GPU 실행 결과다. EER 0.40을 고정 임계값 정확도 60%라고 해석하지 않는다. AUC 0.44만을 근거로 출력 방향을 반전하는 후보도 만들지 않는다. 표본이 작고 출처·연주자가 편중되어 있다.

세 로그 모두 입력/출력 20행, 실패 0건. GPU는 Tesla T4. 외부 프로세스 시간은 각각 약 72.9초, 63.5초, 65.8초이며 모델 로딩 및 프로세스 비용이 포함된다. 순차 실행에 따른 캐시·초기화 비용 차이가 있어 이 차이만으로 속도 개선을 확정하지 않는다. L4 평가 서버 전체 시간으로 환산하지 않는다.

## 검증과 한계

ZIP CRC 검사 통과, ERROR.txt 없음. 결과에 정답, 예측, 설정, 환경, 기준 스크립트, 로그가 포함됐다. 모델 해시 셀의 함수/문자열 이름 충돌은 앞선 사용자 오류 보고로 수정했다. 이번 ZIP은 그 후 추론이 세 설정 모두 완료됐음을 보여준다.

원본 ZIP SHA-256: `c3a50664a43a81da135eba07bb295458bf37cd3c81fce2a21efecc7a2cb99136`.
반환된 manifest와 정답은 준비한 로컬 원본과 일치했다. 세 예측의 ID·행·확률 범위·유한값을 검사하고 EER/AUC를 재계산해 summary.csv와 일치함을 확인했다. 기준 스크립트는 현재 submit/script.py와 일치하고, 설정별 스크립트 해시도 기준 소스와 CONFIG에서 재구성한 해시와 일치했다.

File EER은 FAKE=1, drop_intermediate=False, FPR/FNR 차이 최소 지점의 평균으로 계산한다. 성분 정답이 없으므로 Music EER·Voice EER·CPS·대회 Score를 만들지 않는다. 음악 파일 표본에서 측정한 File EER이지 Music EER이 아니다.

현재 표본은 진짜 음악 10개(연주자 3그룹), 생성 음악 10개(생성기 5종)의 8초 크롭이다. 작은 진단 데이터에서의 실패는 생성 음악 전반의 성능이나 대회 성적을 확정하지 않는다. 이번 데이터를 다시 튜닝한 뒤 독립 확인셋으로 재사용하지 않는다.

## 다음 실험 방향

집계 방식만 더 많이 바꾸는 탐색은 우선순위를 낮춘다. 다음에는 실제 음악의 연주자와 생성 설명 그룹을 늘리고 학습/개발/확인 분할을 고정한 뒤, 음악 판별용 표현 또는 작은 프로브가 기존 모델보다 유효한지 평가한다. 현재 20개만으로 프로브를 학습해 같은 20개에서 좋은 점수를 보고하지 않는다.

FILE 개선을 최종 후보로 채택하려면 실제 음성 및 혼합 파일의 정답을 갖춘 별도 세트에서 전체 산식과 회귀를 확인해야 한다. 일일 제출 3회는 유효한 후보가 준비되었을 때 활용하며 이번 미개선 후보로 슬롯을 채우지 않는다.

AI 활용 기록: 2026-09-15, Codex가 사용자 결과 ZIP·실행 로그·예측·설정 확인 및 실험 결과 판단을 수행했다. 추가 학습·새 GPU 실행·대회 제출은 수행하지 않았다.

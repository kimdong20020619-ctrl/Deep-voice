# 파일 프로브 첫 학습 결과 — 2026-09-15

## 결론

특징 추출·학습·개발셋 후보 선택·확인셋 평가까지 완료됐다. 작은 음악 중심 확인셋에서는 학습한 프로브가 기존 DF-Arena 원본/max보다 개선됐다. 추가 검증을 진행할 가치가 있으나 대회 제출 모델로 즉시 채택할 근거는 부족하다. 이번 가중치를 보존하고 확인셋에 재튜닝하지 않는다.

원본 결과: `C:\Users\kimdo\Downloads\file_probe_results.zip`.
SHA-256: `2fca9d479001b47c165b739d13c5eff78e70a3219064dd14357a2cf0fbbdede9`.
출력 포함 노트북: `C:\Users\kimdo\Downloads\colab_file_probe (1).ipynb`.

## 실측 결과

| 항목 | 수치 | 출처 URL | 기준연도 | 비고 |
|---|---|---|---|---|
| 개발셋 기존 모델 | File EER 0.75 / AUC 0.1875 | 로컬 결과 ZIP: holdout.json, predictions.csv | 2026 | 16개 |
| 개발셋 선택 프로브 | File EER 0.125 / AUC 0.859375 | 같은 로컬 결과 ZIP | 2026 | C=0.1 선택에 사용 |
| 확인셋 기존 모델 | File EER 0.375 / AUC 0.625 | 같은 로컬 결과 ZIP | 2026 | 16개 |
| 확인셋 선택 프로브 | File EER 0 / AUC 1 | 같은 로컬 결과 ZIP | 2026 | 개발셋에서 선택한 모델을 평가 |

위 수치는 외부 성능 자료가 아닌 사용자 GPU 실행 결과다. 학습은 52파일의 104세그먼트, 특징 차원 1280. 개발 후보 C=0.001/0.01/0.1의 EER은 각각 0.625/0.375/0.125로, 예정된 기준에 따라 C=0.1이 선택됐다. 특징 추출 기록은 Tesla T4, torch 2.11.0+cu128, 약 67.34초이며 모델 로딩 등이 포함된다. 대회 전체 실행 시간으로 환산하지 않는다.

## 직접 확인한 항목

- ZIP CRC 통과, 특징 NPZ 84개, 후보 선택·확인 결과·가중치·예측·로그 존재.
- 반환 manifest와 준비한 번들 manifest가 동일. 기준 코드와 모델 해시 목록도 번들과 일치.
- 원곡/연주자/생성 설명·생성기·해시의 분할 중복 검사 통과.
- 모든 특징이 유한하고 각 특징에 기록된 크롭 해시가 manifest와 일치.
- 표준화 평균·표준편차를 train 특징에서 재계산해 저장 가중치와 정확히 일치함을 확인.
- 개발/확인 예측 32행의 ID·정답·분할을 대조하고, 특징과 저장 가중치로 예측값 및 File EER/AUC를 재계산해 기록과 일치함을 확인.

최초 표준화 대조는 검토 코드에서 입력을 float64로 먼저 바꿔 계산하여 약 1e-6 수준 차이로 엄격한 검사가 실패했다. 실제 학습은 float32 특징에서 통계를 계산한 뒤 float64로 저장한다. 동일 계산 순서로 다시 검사하니 평균·표준편차가 원소별 정확히 일치했다. 허용 오차를 무작정 늘려 통과시킨 것이 아니다.

## EER 0의 정확한 의미와 한계

확인셋에서 진짜 파일의 가장 높은 프로브 확률은 약 0.73611, 가짜 파일의 가장 낮은 확률은 약 0.79184다. 이 16개에서는 두 클래스의 점수 순서가 분리된다. 그러나 진짜 일부의 확률이 0.5보다 높기 때문에 고정 임계값 0.5에서 정확도 100%라는 뜻은 아니다. 확인 데이터에서 발견한 임계값을 새 데이터에 맞는 값으로 채택하지 않는다.

확인셋의 실제 연주자는 4그룹이고 생성기는 stable_audio_open 한 종류다. 1280차원에 비해 학습 표본이 작으며 다른 언어·음성·가창·혼합·긴 파일·압축·전화 조건에 대한 일반화를 입증하지 못했다. 별도의 학습 재현 실행이나 전체 제출 파이프라인 GPU 비교를 수행한 것은 아니다.

음원 전체에 peak 정규화가 적용된 실험이다. 기존 제출 추론에 같은 전처리가 적용되는지와 그 변경이 기존 음성/음악/존재 지표에 미치는 영향은 별도 검증해야 한다. 가중치만 FILE 경로에 넣으면 같은 입력 조건이라고 볼 수 없다.

## 다음 단계

1. 이번 선택 가중치·설정·데이터와 결과를 보존한다. 기존 제출 ZIP은 변경하지 않는다.
2. 이번 확인셋으로 C·가중치·임계값을 더 조정하지 않는다. 새로운 원곡·연주자·설명 그룹으로 추가 검증한다.
3. 고정한 프로브를 원래 음량과 학습 시 정규화 조건에서 비교해 전처리 의존성을 확인한다. 이는 성능 확인과 별도의 경로 일치 검사로 구분한다.
4. 실제 음성·생성 음성·혼합 파일에서 파일 판별 회귀를 확인하고, 성분 정답을 갖춘 데이터에서 전체 Score를 평가한다. 음악 중심 데이터로 학습한 FILE 프로브를 모든 음원에 적용하면 음성 판별을 해칠 수 있다.
5. 전체 경로·출력 형식·오프라인 실행·시간/메모리·출처 확인을 통과한 후보만 일일 제출 슬롯에 사용한다.

AI 활용 기록: 2026-09-15, Codex가 반환된 특징·가중치·분할·예측·실행 로그를 검토하고 train 표준화 및 예측/EER/AUC를 재계산했다. 추가 모델 학습·확인셋 튜닝·대회 제출은 수행하지 않았다.

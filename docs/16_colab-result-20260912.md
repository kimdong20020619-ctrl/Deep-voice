# Colab 첫 실행 결과 — 2026-09-12

## 근거와 결론

사용자가 저장한 `C:\Users\kimdo\Downloads\colab_verify.ipynb`의 실행 출력 확인. 파일 SHA-256은 `ba8817129b4230c706c4038b1848008ae5e884e4fe8a33679c9ba2612be5a259`이다. 원본 다운로드 파일은 수정하지 않았다.

코드 셀 9개에 실행 번호 1~9가 있고 notebook error 출력은 없다. 모델 실행과 제한된 포맷 검사는 성공했다. 정확도 개선, L4 서버 60분 이내 완주, 실제 혼합 음원 일반화는 확인하지 못했다.

## 실제 출력

수치는 위 로컬 노트북 실행 출력에서 읽은 실측이다. 외부 벤치마크 인용이 아니다.

- GPU: Tesla T4, 15360 MiB. torch 2.11.0+cu128, torchaudio 2.11.0+cu128, transformers 5.16.1. 설치 경로는 Python 3.13이다. 평가 서버 환경과 동일하지 않다.
- 모델 3종 모두 제공 SHA256SUMS와 일치한다는 출력 확인.
- 기본 더미 3개: returncode 0, 실패 0건, 스크립트 보고 전체 46초, 모델 로드 42.7초. 출력 형식 검사 통과.
- 배치 대 단일 경로 표본 최대 편차 0.000000. bf16 대 fp32 표본 최대 편차 0.00157. 표본 검증이지 모든 음원에서 동등하다는 증명은 아니다.
- 포맷/길이 변형 18개: returncode 0, 실패 0건, 스크립트 보고 전체 108초, 모델 로드 33.4초, 외부 프로세스 측정 115.7초.
- 기본 더미 3개의 FILE/VOICE 출력은 모두 0.999144, MUSIC은 0.0. VP는 0.650272, MP는 0.060305. 동일 더미의 결과이며 정답 라벨이 없어 오탐 또는 정탐으로 판정할 수 없다.

## 발견한 시간 계산 오류와 수정

기존 노트북 7절은 길이마다 새 프로세스를 실행하고 외부 측정 시간에서 모델 로드 로그만 뺀 값을 순수 추론 시간으로 간주했다. 이 차이에는 Python import·프로세스 시작/종료·저장 비용이 남는다. 짧은 파일에서 이 고정 비용이 오디오 초당 비율을 크게 만들고, 비율 중앙값을 전체 데이터에 적용하면서 근거 없는 장시간 추정으로 이어졌다.

출력된 121.9~487.8분 추정은 제출 서버 실행 시간이나 시간 초과의 증거로 쓰지 않는다. 그렇다고 제한 통과를 입증한 것도 아니다. 서로 다른 GPU와 비대표 더미 데이터 때문에 기존 제출 화면의 소요 시간과 직접 비교할 수 없다.

`scripts/make_verify_notebook.py`에서 이 환산과 초과 판정을 제거했다. 남은 수치는 새 프로세스 실행 비용으로 표시한다. 개별 측정 프로세스 실패도 확인하도록 보완하고 노트북을 재생성했다. 모델·추론 코드·제출 ZIP은 변경하지 않았다. 원본 결과의 잘못된 추정은 재현 근거로 보존한다.

## 다음 작업

같은 더미 검사를 다시 요청하지 않는다. 다음 실험은 실제 음성·음악·혼합 파일로 경로별 동작을 확인하고, 한 번 로드한 모델에서 여러 파일을 연속 처리한 시간을 측정해야 한다. 파일별 정답 라벨과 원본 그룹 분리까지 갖춰야 성능 비교가 가능하다.

Colab 런타임을 보존할 수 있다면 모델 다운로드를 반복하지 않고 이어가는 편이 효율적이다. 런타임이 종료되어도 이번 결과는 로컬 노트북에 남아 있다. 현재 결과만으로 새 제출물을 만드는 단계는 아니다.

AI 활용 기록: 2026-09-12, Codex가 사용자 Colab 실행 결과 확인, 환경·완주·가중치 검증 기록, 시간 환산 오류 분석과 생성기 수정, 노트북 재생성을 수행했다.

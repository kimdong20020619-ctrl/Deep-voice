# CLAUDE.md — Deep-voice

DACON 236749 「딥보이스 범죄 대응 AI 탐지 모델 경진대회」 작업 레포.
전역 지침(`~/.claude/CLAUDE.md`)이 우선하고, 이 파일은 대회 고유 제약만 담는다.

## 절대 제약 — 모든 설계 결정의 상한선

| 제약 | 값 | 위반 시 |
|---|---|---|
| 제출 zip 크기 | ≤ 10 GB (**해제 후 ≤ 32 GB**) | 제출 거부 |
| 패키지 설치 시간 | ≤ 10 분 | 실행 실패 |
| 추론 시간 | ≤ 60 분 / 1,200 파일 (**파일당 3초**) | 실행 실패 |
| 일일 제출 | 3 회 | — |
| 언어 | Python 만 | 규정 위반 |
| 인코딩 | 모든 CSV·제출 파일 UTF-8 | 규정 위반 |

**목표 추론 시간은 40분이다.** 60분은 상한이지 목표가 아니다. 평가 서버 스펙을 모르므로 안전마진을 둔다.

## 실행 환경 (확정)

**Ubuntu 22.04.5 · Python 3.11.15 · CUDA 12.8 · 6 vCPU · 28 GB RAM · NVIDIA L4 22.4 GiB · 오프라인**

- 인터넷은 패키지 설치 시에만 열린다. 모델 가중치는 전부 zip에 동봉하고
  `HF_HUB_OFFLINE=1` · `TRANSFORMERS_OFFLINE=1`을 설정한다.
  `from_pretrained("hf-repo-id")` 같은 런타임 다운로드를 추론 경로에 남기지 않는다.
- L4가 보장되므로 CPU 폴백은 불필요하다. 대신 **L4 22.4GB에 들어가는지**를 항상 확인한다.
- **`requirements.txt`는 가능한 한 비워둔다.** torch 2.7.1+cu128 · torchaudio · demucs 4.0.1 ·
  panns-inference · librosa · transformers 4.57.6 등이 이미 설치돼 있고, 다른 버전을 명시하면
  설치 오류가 난다. 목록은 `docs/06_scoring-and-submission.md` 7절.
- 제출 zip 루트에 `model/` · `script.py` · `requirements.txt` 3개만 둔다.
  **최상위 폴더를 감싸면 구조 불일치로 설치 오류.** 결과는 `output/submission.csv`.
- 오류 비용이 다르다 — 설치 오류는 제출 횟수 차감 없음, **런타임 오류는 3회 중 1회를 태운다.**
- 배포 더미는 WAV 3개뿐이지만 실제 평가 데이터는 MP3/WAV/FLAC 혼재 + 모노/스테레오 혼재 +
  일부 전화채널이다. 디코딩 실패가 곧 0점이므로 로더는 포맷 예외를 삼키지 말고 명시적으로 처리한다.

## 점수 구조 (확정) — 모든 자원 배분의 근거

```
Score = 0.9 × ADS + 0.1 × CPS
ADS   = 0.5×(1−File EER) + 0.2×(1−Voice EER) + 0.3×(1−Music EER)
CPS   = 0.5×Voice Presence AUC + 0.5×Music Presence AUC
```

**총점 기준 실효 가중치**: FILE **0.45** · MUSIC **0.27** · VOICE **0.18** · 존재 2개 합계 0.10

1. **음악 위조도(0.27)가 음성(0.18)보다 무겁다.** 베이스라인이 여기를 음성 모델로 때우고 있다.
2. `FILE_FAKE`(0.45)가 음악 헤드에 물려 있다 → **음악 헤드 개선이 실질 최대 레버리지**.
3. 존재 탐지(0.10)는 PANNs zero-shot으로 포화(0.989). **투자 금지.**
4. **EER은 순위 기반** → 확률 캘리브레이션은 점수에 영향 없다. 시간 쓰지 않는다.
5. **추론 속도는 점수에 반영되지 않는다.** 60분은 통과/실패 조건일 뿐이다.
   시간이 남으면 정확도(앙상블·TTA·큰 모델)에 재투자한다.

상세: `docs/06_scoring-and-submission.md`

## 절대 금지 (실격 사유)

- 비공개 평가 데이터를 이용한 추가 학습·튜닝·Pseudo-Labeling
- **다른 파일의 정보·예측값·통계를 이용한 예측 또는 보정**
  → 테스트셋 전체 분포 정규화·순위 변환 트릭은 EER 지표상 유혹적이지만 **명시적 금지**다.
     세그먼트 집계는 한 파일 내부의 일이므로 허용.
- 평가 데이터셋 유출 시도

## 작업 규칙

- 라이선스는 **채택하는 순간** `docs/03_license-ledger.md`에 한 줄 적는다. 소급 정리하지 않는다.
  2차 평가에서 모든 데이터·모델의 출처 명시가 의무다.
- 리더보드 제출은 **변경점 1개씩만** 반영하고 `docs/04_experiment-log.md`에 기록한다. 일 3회뿐이다.
- 로컬(i3-8145U / RAM 8GB / GPU 없음)에서는 학습하지 않는다. 코드 편집·문서·zip 패키징 전용.
  로컬 Python 3.14는 torch 휠 호환이 불안하므로 검증용 CPU 추론은 별도 Python 3.11 venv에서 한다.
- 학습은 Kaggle(주 30 GPU시간, 세션 12시간 제한). 체크포인트 재개가 가능하게 짠다.
- 커밋·푸시는 사용자가 직접 한다.
